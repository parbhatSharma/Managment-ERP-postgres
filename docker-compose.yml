services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: school
      POSTGRES_PASSWORD: school
      POSTGRES_DB: school_erp
    ports: ["5432:5432"]
    volumes: [school_erp_data:/var/lib/postgresql/data]
volumes:
  school_erp_data:
