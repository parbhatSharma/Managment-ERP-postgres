import { app } from './app.js'; import { env } from './config/env.js'; import { prisma } from './lib/prisma.js'; const server=app.listen(env.PORT,()=>console.log(`Brightwood API on http://localhost:${env.PORT}`)); const stop=async()=>{server.close();await prisma.$disconnect();process.exit(0)}; process.on('SIGINT',stop);process.on('SIGTERM',stop);
