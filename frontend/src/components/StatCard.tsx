import { StyleSheet,Text,View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import { Card } from './ui'; import { colors } from '@/theme'; export function StatCard({label,value,icon,tone='blue'}:{label:string;value:string|number;icon:keyof typeof Ionicons.glyphMap;tone?:'blue'|'green'|'orange'|'purple'}){const c={blue:colors.primary,green:colors.green,orange:colors.orange,purple:colors.purple}[tone];const bg={blue:colors.primarySoft,green:colors.greenSoft,orange:colors.orangeSoft,purple:colors.purpleSoft}[tone];return <Card style={s.card}><View style={[s.icon,{backgroundColor:bg}]}><Ionicons name={icon} size={21} color={c}/></View><Text style={s.value}>{value}</Text><Text style={s.label}>{label}</Text></Card>} const s=StyleSheet.create({card:{width:'48%',gap:7},icon:{width:40,height:40,borderRadius:13,alignItems:'center',justifyContent:'center'},value:{fontSize:27,fontWeight:'800',color:colors.ink},label:{fontSize:13,color:colors.muted,fontWeight:'600'}});
