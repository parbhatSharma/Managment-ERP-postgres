import { StyleSheet,Text,View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import { Card,Badge } from './ui'; import { colors } from '@/theme'; import type { Announcement } from '@/types'; export function AnnouncementCard({item}: {item:Announcement}){return <Card style={s.card}><View style={s.top}><View style={s.icon}><Ionicons name="megaphone-outline" size={19} color={colors.primary}/></View>{item.pinned&&<Badge label="Pinned" tone="blue"/>}</View><Text style={s.title}>{item.title}</Text><Text numberOfLines={3} style={s.body}>{item.body}</Text><Text style={s.date}>{new Date(item.publishedAt).toLocaleDateString()}</Text></Card>} const s=StyleSheet.create({card:{gap:10},top:{flexDirection:'row',justifyContent:'space-between'},icon:{width:38,height:38,borderRadius:12,backgroundColor:colors.primarySoft,alignItems:'center',justifyContent:'center'},title:{fontSize:17,fontWeight:'800',color:colors.ink},body:{fontSize:14,lineHeight:21,color:colors.muted},date:{fontSize:12,fontWeight:'600',color:'#9AA5B4'}});
